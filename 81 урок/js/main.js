const numbers = [0, 1, 2, 3, 4, 5, 6, 7]
for (let i = 0; i< numbers.length; i= i+1) {
    if(numbers[i]%2 == 0){
        console.log(numbers[i])
    }else{
    continue
    }
}
const colors = ['red', 'orange', 'yellow', 'green', 'light-blue', 'blue, purple']
for (let l = 6; l>=0; l=l-1) {
    console.log(colors[l])
}
